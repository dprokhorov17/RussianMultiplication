<Window x:Class="RussianMultiplication.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:RussianMultiplication"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        xmlns:viewModels="clr-namespace:RussianMultiplication.ViewModels"
        xmlns:validators="clr-namespace:RussianMultiplication.Validators"
        mc:Ignorable="d"
        Style="{StaticResource MaterialDesignWindow}"
        d:DataContext="{d:DesignInstance viewModels:MainViewModel}"
        Title="Russian Multiplication" Height="350" Width="525">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <StackPanel Orientation="Horizontal" Margin="0,0,0,10">
            <TextBox Width="100" Margin="0,0,10,0" materialDesign:HintAssist.Hint="Number 1">
                <TextBox.Text>
                    <Binding Path="Number1" UpdateSourceTrigger="PropertyChanged" ValidatesOnDataErrors="True"
                             ValidatesOnExceptions="True">
                        <Binding.ValidationRules>
                            <validators:IntValidationRule />
                        </Binding.ValidationRules>
                    </Binding>
                </TextBox.Text>
            </TextBox>

            <TextBox Width="100" Margin="0,0,10,0" materialDesign:HintAssist.Hint="Number 2">
                <TextBox.Text>
                    <Binding Path="Number2" UpdateSourceTrigger="PropertyChanged" ValidatesOnDataErrors="True"
                             ValidatesOnExceptions="True">
                        <Binding.ValidationRules>
                            <validators:IntValidationRule />
                        </Binding.ValidationRules>
                    </Binding>
                </TextBox.Text>
            </TextBox>

            <Button Content="Multiply" Command="{Binding MultiplyCommand}" />
        </StackPanel>

        <TextBlock Grid.Row="1" Text="Result:" FontWeight="Bold" />
        <TextBlock Grid.Row="1" Margin="60,0,0,0" Text="{Binding Result}" />

        <DataGrid Grid.Row="3" AutoGenerateColumns="False" ItemsSource="{Binding Steps}" Margin="0,10,0,0"
                  CanUserAddRows="False"
                  CanUserReorderColumns="False"
                  CanUserResizeRows="False"
                  CanUserDeleteRows="False"
                  CanUserSortColumns="False">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Left" Binding="{Binding Left}" IsReadOnly="True"/>
                <DataGridTextColumn Header="Right" Binding="{Binding Right}" IsReadOnly="True" />
                <DataGridCheckBoxColumn Binding="{Binding Add}" IsReadOnly="True">
                    <DataGridCheckBoxColumn.Header>
                        <TextBlock Text="Shall Add" 
                                   ToolTipService.ToolTip="Checked, if the value in the right column shall not be added to the final result" />
                    </DataGridCheckBoxColumn.Header>
                </DataGridCheckBoxColumn>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</Window>